<%- include('bootheader') -%>
<%- include('menubar') -%>
<div class="p-3 mb-4 bg-light">
  <div class="container-fluid ">
    <h1 class="">Create a Course</h1>
    <p class="border-bottom pb-2"> Enter a new course name, starting and ending dates,
      and whether the course is in non-grading mode.
    </p>
    <div class="p-2">

      <form method="post" action="createNewCourse">
        <div class="col-lg-8">
          <div class="input-group mb-3">
            <label class="input-group-text">Course Name</label>
            <input type="text" name="courseName" class="form-control">
          </div>
        </div>
        <br>
        <% let now = new Date();
           let in4months = new Date(now.getTime() + 1000*3600*24*120);
          %>
        starting date: <input type="text" name="startDate" 
        value="<%= now.toISOString().slice(0,10) %>">
        
         <br>
         ending date: <input type="text" name="stopDate" 
         value="<%= in4months.toISOString().slice(0,10) %>">
         
         <br>
         <input type="checkbox" 
                 name="nonGrading" 
                 value="false"
           >
         non-Grading mode - student work will be graded externally and uploaded to the course
         students will not be able to see the problems or the answers. Uncheck the box
         if you want to grade the students' work in the course using this App.
         <br>
         <hr>

        <div class="form-check">
          <input class="form-check-input" id="norobot" type="checkbox" name="norobot">
          <label class="form-check-label" for="norobot">I am not a robot </label>
        </div>
        <div class="form-check mb-3">
          <input class="form-check-input" type="checkbox" id="robot" name="robot">
          <label class="form-check-label" for="robot">I am a robot </label>
        </div>
        <input class="btn btn-md btn-danger" type="submit" value="Create the new course">
      </form>
      <hr>
      <p>
        Select a name which will easily identify your course.
        For example: give the Course ID, section, and semester, e.g. COSI10a-02 Spr20
        The course name will only be visible to you and the students who
        register in your course.
      </p>
      <p> Students register using a 7 digit pin number which will be
        created for you when you make this course.
      </p>
    </div>
  </div>
</div>

<%- include('bootfooter') -%>
<%- include('htmlfoot') -%>