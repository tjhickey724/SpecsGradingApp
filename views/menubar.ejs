<nav class="shadow navbar navbar-expand-lg navbar-light bg-primary border-bottom-0">
  <div class="container-fluid">
    <a class="navbar-brand text-light text-wrap" href="/">Mastery Learning/Specs Grading/Peer Review App</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo01"
      aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
      <ul class="navbar-nav mb-2 mb-lg-0 ms-auto">
        <li class="nav-item">
          <a class="nav-link <% if (routeName == " index" || routeName==" login" ) { %> active fw-bold <% } %> "
              href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link <% if (routeName == " stats") { %> active fw-bold <% } %> " href="/stats">
              Stats </a>
        </li>
        <% if (loggedIn) { %>
          <li class="nav-item">
            <a class="nav-link <% if (routeName == " profile") { %> active fw-bold <% } %> " href="/profile">
                Profile (<%= user.googleemail %>)
            </a>
          </li>
          <li class="nav-item">
            <a class="mt-1 btn btn-outline-dark btn-sm" href="/logout"> Logout</a>
          </li>
          <% } else { %>
            <li class="nav-item">
              <a class="mt-1 btn btn-outline-dark btn-sm" href="/auth/google"> Login</a>
            </li>
            <% } %>
      </ul>
    </div>
  </div>
</nav>